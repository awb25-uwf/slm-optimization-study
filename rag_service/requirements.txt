# Core API
fastapi==0.139.2
uvicorn[standard]==0.49.0

# Vector store client + embeddings
chromadb==1.5.9
sentence-transformers==5.6.0

# Metrics / monitoring
prometheus-client==0.25.0
psutil==7.2.2

# Validation (fastapi dependency, pinned explicitly)
pydantic==2.13.4

# NOTE: torch is intentionally NOT pinned here. In Docker it is installed as
# a separate, earlier layer using the CPU-only wheel index (see Dockerfile)
# to avoid pulling several GB of CUDA libraries this service doesn't need.
# For a local (non-Docker) install, run this first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then install this file.
