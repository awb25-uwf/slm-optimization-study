# ML / training stack
torch==2.11.0
transformers==4.53.3
peft==0.19.1
trl==0.20.0
datasets==5.0.0
huggingface_hub==0.36.2
accelerate==1.14.0   # not imported directly, but required as the training backend by trl.SFTTrainer / transformers

# Serving (serve_api.py)
fastapi
uvicorn[standard]

# HTTP clients (calls to local Ollama server / misc APIs)
httpx==0.28.1
requests==2.32.5

# Document/PDF ingestion (extract_and_chunk.py)
python-docx
pypdf

# Metrics / monitoring
prometheus-client==0.20.0
psutil==7.2.2

# Validation (fastapi dependency, pinned explicitly)
pydantic==2.13.4

# Optional, Jetson-only: enables GPU power/temp/clock readings in
# resource_monitor.py. Silently no-ops if unavailable, so safe to omit
# on non-Jetson machines.
# jetson-stats==7.2.0
